//Test program. Simple stuff

#include <stdio.h>

main()
{
	float a=1, b=2, total;

	total=a+b;
	printf("The sum is %f", total);

	total=a-b;
	printf("\nThe difference is %f", total);

	total=a*b;
	printf("\nThe product is %f", total);

	total=a/b;
	printf("\nThe quotient is %f", total);
}